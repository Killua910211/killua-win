import { basicPath } from './learning-path';
import { coreQuestions, philosophyNodes, traditions } from './tree';

/**
 * 传统地图下真正的线索条数：五条导航自身不算，算它们各自展开的历史时段与思想线索。
 */
const traditionThreadCount = traditions.reduce(
  (total, tradition) => total + (tradition.children?.length ?? 0),
  0,
);

/**
 * 本版覆盖范围。
 *
 * 上一版这里写着「本版范围内无未建来源账条目」和「这些缺口不被伪装成已核验结论」。
 * 本轮系统审查证明这两句都不成立：来源里有 9 条失效链接、两处凭空写出的章节号
 * （SEP Philosophy of Technology 没有 §6.2，SEP Mohism 没有 §2.1）、一个已经不存在
 * 的域名，而页面上每条来源都盖着硬编码的「已核验」。
 *
 * 所以这份文案改成描述实际状态，包括还没做到的部分。写「已完成」比缺口本身更糟。
 */
export const philosophyCoverage = {
  core: {
    label: '核心｜问题与历史并列',
    /*
      三个数字以前是写死的（23／18／55）。它们当时是对的，但 data.json 一加节点就会
      静默失真——这份文案的作用正是说清范围，说错了比不说更糟，所以改成从节点树数。
    */
    detail:
      `本版包含 ${coreQuestions.length} 个核心问题和 ${traditionThreadCount} 条传统／历史线索，共 ${philosophyNodes.length} 个节点。问题页负责比较论证，历史页负责把问题放回语言、文本、制度与传播之中；两者是平行的两条轴，不是主线与附录。`,
  },
  reading: {
    label: '阅读顺序｜有主线，也有支线',
    /* 步数同样不写死：这份文案的作用是说清范围，说错了比不说更糟。 */
    detail:
      `有一条 ${basicPath.steps.length} 步的推荐路线，从一个日常判断起步，最后落到死亡与意义；主线之外的 ${coreQuestions.length - basicPath.steps.length} 个核心问题按「你关心什么」列在同一页，每一条都写明它默认你已经有哪个区分。问题域和传统导航各自给出建议顺序并逐条说明理由，条目页底部的前后页跟着同一条顺序走。任何一页都可以单独打开：需要前置理解的页面，开头会写明本页默认你已经知道什么，并附一句不跳转也够用的回顾。`,
  },
  relations: {
    label: '知识关系｜带理由的连接',
    detail:
      '问题之间的连接不是一份「相关文章」清单。七种关系各自要求作者回答一个具体问题：前置理解要说清对面那一页的哪个区分是本页的前提，跨传统比较要同时写出可比的争点和不可等同之处。说不出理由的连接不进关系层。',
  },
  verified: {
    label: '来源核验｜逐条标状态',
    detail:
      '来源的核验状态逐条显示：已核验、待核验，或链接失效待更换。「已核验」意味着本轮实际打开过那个页面、标题相符、章节定位真的存在；做不到就标为待核验，不给整页的来源统一盖一个戳。',
  },
  gaps: {
    label: '已知缺口｜不假装补齐',
    detail:
      '来源仍高度集中在英文的 Stanford Encyclopedia；部分汉文佛教原典和王弼中文研究已有核验，但儒道、印度、伊斯兰与非洲哲学的原典及区域专门史仍明显不足。实用主义、逻辑经验主义与批判理论已补入门争点，尚不构成完整学派史。部分条目的立场清单仍偏内部分歧，最强的外部反对还没到位。这些缺口逐条写在各条目页末尾的「来源、核验与修订记录」里，不折算成完成度。',
  },
  excluded: {
    label: '暂不纳入',
    detail:
      '未经核验的名言、无法定位的页码与章节、用单一民族／宗教／地域标签概括整种传统，以及把即时政策新闻包装成恒久哲学结论。',
  },
} as const;
