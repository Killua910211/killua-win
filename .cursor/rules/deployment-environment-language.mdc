---
description: 统一 Site 测试环境与正式网站的部署称呼
alwaysApply: true
---

# 部署环境称呼

- 部署到 Codex Sites 的地址（包括 `*.chatgpt.site`）统一称为“测试环境”或“部署到测试环境”。不要称为“线上网页”“线上环境”或“正式上线”。
- 只有部署到正式网站 `https://www.killua.win/`，才称为“部署上线”“正式网站上线”或“正式环境”。
- 向用户汇报 Site 部署结果时，优先使用上述中文称呼；即使底层工具把 Site 发布动作标记为 production，也不改变用户-facing 的环境称呼。
- 如果一次操作同时涉及 Site 测试环境和 `https://www.killua.win/`，必须分别说明两个环境，不得把测试环境描述成正式上线。
